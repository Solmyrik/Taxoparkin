// const strategy = document.querySelector('.strategy');
// const valueStrategy = strategy.getBoundingClientRect().y;

// console.log(valueStrategy);

// document.addEventListener('scroll', (e) => {
//   if (strategy.getBoundingClientRect().y < 0) {
//    let currentPosition = window.pageYOffset;

//    // Затем останавливаем скролл на 1 секунду
//    window.scroll(0, currentPosition);
//    setTimeout(function() {
//      // Возвращаем позицию обратно после 1 секунды
//      window.scroll(0, currentPosition);
//    }, 1000); // 1000 миллисекунд = 1 секунда
//   }
// });
